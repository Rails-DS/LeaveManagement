<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"> <h3 class="h3-title">Dayspring</h3></a>
	 <div class="collapse navbar-collapse" id="navbarSupportedContent">
    	<ul class="navbar-nav for-float ">
	     	<li class="nav-item">
	     		<%= link_to 'Approve/Reject Leave', leave_request_path, {class:"anchor-item"} %>
	     	</li>
	      	<li class="nav-item">
	     		<%= link_to 'Add Team ', teams_path, {class:"anchor-item"} %>
	     	</li>
	      	<li class="nav-item">
	        	<%= link_to 'Add User', new_user_path, {class:"anchor-item"}%>
	      	</li>
	     	<li class="nav-item">
	     		<% if !@user.is_tl %>
					<%= link_to 'Logout', logout_path, {class:"anchor-item anchor-item-logout"}%>
				<%end%>
	     	</li>
    	</ul>
    </div>
</nav>	

<h4 style="margin-left: 73px;margin-top: 46px";>USER : <strong> <%= @user.email %> </strong></h4>


<div class="container my-auto ">
<table class="table table-borderless table-hover">
	<tr>
		<th>Name</th>
		<th>Email</th>
		<th>Joined Date</th>
		<th>Team</th>
		<th>Role</th>

	</tr>
<% @users.each do |user| %>
	<% if user.is_admin == false %>
	<tr>
		<td><%= link_to user.name,user_path(user.id) %></td>
		<td><%=user.email %></td>
		<td><%=user.joiningDate.strftime("%Y-%m-%d")%></td>
		<td><%=user.team.teamName %></td>
		<td><%=user.role.roleName %></td>
		<td><%= link_to "Edit", edit_user_path(user.id),{class:"Edit"}%></td>
		<td><%= button_to "Delete", user_path(user.id), method: :delete%></td>
	</tr>
	<%end%>
<%end%>

</table>

</div>

<%#= link_to 'logout', logout_path, {class:"btn btn-danger"}%>
<%#= link_to 'Add User', new_user_path%>
<%#= link_to 'Add Team ', teams_path%>
<%#= link_to 'Approve/Reject Leave', leave_request_path%>




